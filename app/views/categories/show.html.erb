<%= render 'partials/content_header', back: true, backPath: categories_path, title: '', add: 0, text: '', path_action: @category, item: 'la categoría' %>
<section class="content">
  <div class="box">
    <div class="box-body">
      <div class="row form-horizontal">
        <div class="col-md-6">
          <div class="form-group">
            <div class="show-property">
              <label>
                <span class="show-key">Categoría:</span>
                <span class="show-value" >  <%= @category.name %></span>
              </label>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>


