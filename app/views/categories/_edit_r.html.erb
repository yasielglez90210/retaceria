<div class="modal-dialog">
  <div class="modal-content">
    <%= form_with(model: @category,html: { style: "display:inline;",autocomplete: "off" }) do |f| %>
        <div class="modal-body">
          <ul class="errors"></ul>

          <div class="form-group">
            <%= f.label :name ,'Categoria', class:"control-label" %>
            <%= f.text_field :name, class: "form-control" %>
          </div>

        </div>
        <div class="modal-footer">
          <%= f.submit 'Guardar', class: "btn btn-primary" ,data: { "disable-with": "Guardando..." } %>
          <%= link_to "Cancel", "#", class: "btn", data: {dismiss: "modal"} %>
        </div>
    <% end %>
  </div>
</div>